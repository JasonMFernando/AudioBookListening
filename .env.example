# Frontend (Vercel)
NEXT_PUBLIC_WORKER_URL=http://localhost:8787

# After deploy, set this in Vercel Project Settings → Environment Variables, e.g.:
# NEXT_PUBLIC_WORKER_URL=https://audio-listening.<your-subdomain>.workers.dev
